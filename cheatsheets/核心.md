# 核心速查

> 本頁拆成幾檔（每檔 ≤150 行）：[核心b](核心b.md)。

語言核心・括號家族・資料結構・印與除錯・jpm/REPL・地雷合輯（Janet 1.41.2 實測）

## 語言核心

### 綁定

```janet
(def x 10)          # 不可變
(var y 20) (set y 30)  # 可變
(let [a 1 b 2] (+ a b)) # 區域 => 3
```

### 函式

```janet
(defn add [a b] (+ a b))
(defn greet [&opt who]        # 可選參數
  (default who "world")
  (string "Hi, " who))
(defn sum [& xs] (reduce + 0 xs)) # 變長
(map |(* $ 2) @[1 2 3]) # |(..) $=首參
```

### 條件

```janet
(if (> x 5) "big" "small")
(when (> x 5) (print "big")) # 只真、多句
(cond (< x 0) "neg"  (= x 0) "zero"  "pos")
(case x 10 "ten" "other")
```

真假：只有 `nil` 與 `false` 是假，`0`／`""`／空集合都算真。

### 迴圈 / 推導

```janet
(loop [i :range [0 3]] (prin i)) # 副作用 012
(each v @[:a :b] (prin v))       # 走訪值
(seq [i :range [0 5] :when (odd? i)]
  (* i 10))  ;=> @[10 30]  推導回陣列
```

## ✦ 括號家族 `() [] {} @`

**`()` 是「呼叫」，`[]`/`{}` 是「資料」。**這是 Janet 跟傳統 Lisp 最不同、也是它 JSON 很順的根本。

```janet
(+ 1 2)           ;() 當呼叫 => 3
[1 2 3]          ;[] => tuple（不可變），內容會求值
[(+ 1 1) 2]     ;=> (2 2)
{:a (+ 40 2)}    ;{} => struct => {:a 42}
```

| 寫法 | 型別 | 可變 |
|---|---|---|
| `[1 2]` | tuple | ✗ |
| `@[1 2]` | array | ✓ |
| `{:a 1}` | struct | ✗ |
| `@{:a 1}` | table | ✓ |

`{:a 1 :b [1 2 3]}` 這種「像 JSON 的東西」就是合法 Janet 字面資料，可內含計算值。

## 資料結構

**可變加 `@`**：`@[]` array · `@{}` table；沒 `@` 的 `[]` tuple · `{}` struct 不可變。

### 陣列 `@[…]`

```janet
(def a @[1 2 3])
(array/push a 4)      ;=> @[1 2 3 4] 原地
(a 0)                ;=> 1  索引當函式
(array/slice a 1 3) ;=> @[2 3]
(map |(* $ $) a)     ;=> @[1 4 9 16]
(filter even? a)     ;=> @[2 4]
(reduce + 0 a)      ;=> 10
```

### 雜湊表 `@{…}`

```janet
(def t @{:name "Alice" :age 30})
(put t :city "Taipei") # 原地寫
(t :name)              ;=> "Alice"
(get t :age :none)    # 第三參 = 預設
(has-key? t :city)    ;=> true
(update t :age inc)   # 用函式更新
(keys t) (values t) (pairs t)
```

### 巢狀 & 合併

```janet
(get-in n [:user :names 0]) # 鑽多層
(put-in n [:user :age] 5)
(merge {:a 1} {:b 2})     ;=> @{:a 1 :b 2}
```

## ✦ 印 / 除錯

> ⚠ **最常踩**：`print` 對 array/table 只印位址 `<array 0x…>`，**不是內容**。

```janet
(pp @[1 2 3])        # pretty-print 內容
(printf "%q" x)      # 內嵌字串：可讀表示
(prin ...) (print ...) # 不換行 / 換行
(doc defn)          # REPL 查文件
```

> ⚠ `%q`/`%p`/`%j`/`%n` 都印 **Janet 表示法**（`%j` 是單行，**不是 JSON**）。真 JSON 只有 `(print (json/encode x))`。

## jpm / REPL

| 指令 | 做什麼 |
|---|---|
| `janet f.janet` | 跑腳本（開發主循環，秒回） |
| `janet -e '…'` | 一行求值 |
| `jpm test` | 跑 `test/` |
| `jpm build` | 編獨立執行檔 `build/` |
| `jpm deps` | 裝 project.janet 的依賴 |
| `jpm install <名/git>` | 裝套件到 `~/.local` |

### nvim · Conjure REPL

| 鍵 | 動作 |
|---|---|
| `,ee` | 求值游標 form |
| `,er` | 求值最外層 form |
| `,eb` | 求值整個 buffer |
| `,ls` / `,lv` | 開 REPL log |

開 `.janet` 檔即自動起 `janet -n -s` REPL。parinfer + 彩虹括號已就緒。
