# D6 docs 12／13／40 審閱

審閱者：fable｜日期 2026-09-26｜範圖：`docs/12-env-環境與動態變數.md`、`docs/12b-切換-env.md`、`docs/12c-dyn.md`、`docs/12d-os-環境變數.md`、`docs/13-symbol-keyword-字串.md`、`docs/40-內建動態變數.md`

## 總評（待補）

## 發現

## 沒來得及看的
<!-- 12 -->
- [低][結構] `docs/12-env-環境與動態變數.md:3` — 說「兩個完全不同的東西」，表格卻列三行，讀者要讀到第 11 行才知道前兩行是同一張表 — 改成「三個詞、兩個東西」或直接說「環境表／dyn 是同一張表，OS 環境變數是另一回事」 — [疑]
- [低][結構] `docs/12-env-環境與動態變數.md:112` — 標題「這一篇拆成三份」，表格列四行（本篇＋b／c／d） — 改「拆成四份」或「另拆三支」 — [疑]
- [低][範例] `docs/12-env-環境與動態變數.md:41-45` — 五個綁定的 meta 全是註解推測的形狀，沒有一段真的 `pp` 輸出；`:doc` 的實際內容是 `"(f a)\n\n說明"`（含簽名），只有 43 行提到，44 行 `:doc (…)` 省略了 — 貼一段實跑輸出（可省 source-map 路徑） — [實測：五行形狀皆正確，`(all-bindings)` 707 個、`all-dynamics` 確有 `&opt env local`]
