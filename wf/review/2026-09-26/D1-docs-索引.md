# D1 docs 索引／導航頁 審閱

審閱者：fable｜日期 2026-09-26｜範圖：`docs/README.md`、`docs/路線圖.md`、`docs/語言細節索引.md`、`docs/主題與-spork-索引.md`、`docs/從別的語言過來-索引.md`、`docs/怎麼做-X.md`、`docs/怎麼做-X-b-資料與排錯.md`

## 總評

連結全通（`python3 bin/md-bundle.py --check` → 「252 篇，0 條壞連結」；路線圖行內 code 提到的 `examples/quickstart.janet`、`data-structures.janet`、`cpp-vs-janet.janet` 也都存在）。`ls docs` 的 96 篇每一篇至少被一份索引收到，沒有孤兒。
抽樣 55 條簡介對照目標篇，絕大多數關鍵字都能在目標篇找到；錯的集中在「怎麼做 X」的任務→篇對應：有四列指到的篇根本沒講那件事（stdin、大端序、重試、環境變數）。最值得先修的是 `怎麼做-X.md:21` 非同步讀 stdin 指到 15，實際內容在 30 的 `spork/stream`。

## 發現

- [中][錯誤] `docs/怎麼做-X.md:21` — 「非同步讀 stdin」教學欄指 [15]，但 `15-ev-channel-net.md` 全文沒有 stdin；講 `stream/lines (stream/make-stdin)` 的是 `30-spork-並行與服務.md:125-137` — 改指 30（或並列 15、30） — [實測 grep]
- [中][錯誤] `docs/語言細節索引.md:24`、`docs/從別的語言過來-索引.md:24` — 都把「⚠ `%d` 遇 3.5 報錯」歸給 43c，但 `43c` 全文無 `%d`，實際在 `43-從-C-C++-過來.md:43` — 兩份索引的 43c 簡介刪掉這條、移到 43 那列 — [實測 grep；`(string/format "%d" 3.5)` 確實報 can not convert]
- [中][錯誤] `docs/怎麼做-X.md:20` — 「處理二進位（讀檔頭、大端序）」教學欄指 [21]，但 21 沒有「大端」「檔頭」「二進位」字樣，只講 int/u64 位元運算 — 教學欄改成「—」或指 18（buffer）並保留 snippet；或在 21 補一段 — [實測 grep]
- [低][錯誤] `docs/怎麼做-X.md:60` — 「重試與逾時」指 [15]，15 只有逾時（`ev/with-deadline`，第 79 節），零筆「重試」 — 列名改「逾時」或另指 snippet 為主 — [實測 grep]
- [低][錯誤] `docs/怎麼做-X.md:30` — 「載入設定（檔案＋環境變數＋驗證）」只指 [29]，29 只講 schema 驗證，環境變數在 `12d-os-環境變數.md` — 補 12d — [實測 grep]
- [低][錯誤] `docs/路線圖.md:82` — 「每篇結尾都有『下一步』連結」不成立：`00-環境與工具鏈.md`、`06-編輯器與-REPL.md`、`17-用-spork-http-打-api.md` 結尾都沒有 — 改成「幾乎每篇」或補三篇的導航 — [實測 grep -L]
- [低][結構] `docs/語言細節索引.md:24-26` vs `docs/從別的語言過來-索引.md:22-38` — 43／44／45 三篇在兩份索引各寫一次、簡介措辭不同（一份合併 43b/43c 進 43 那列，一份拆開），日後改一邊會漂 — 語言細節索引保留一行「43–46 見從別的語言索引」即可，刪重複表列 — [疑]
- [低][結構] `docs/路線圖.md:16-22` vs `docs/README.md:5` — README 說「00 → 06 依序讀完就能上手」，路線圖「最快上手」卻從 07 REPL 開始、之後跳 01→02→03→04→32–40，完全沒提 00（裝環境）、05（jpm）、06 — 至少在步驟 1 前加一句「環境沒裝先看 00」 — [疑]
- [低][結構] `docs/路線圖.md:33-45` — 從 C++ 過來的路線 11 步沒有 03／04／05，走完不會知道 jpm 與 argparse；README:35 那區「日常會用到」也沒被路線圖串進去 — 在步驟 10 後補「要做成專案 → 04、05」 — [疑]
- [低][文風] `docs/怎麼做-X.md:5` — 「其他索引都是按主題排的（語言核心、spork、語言細節）」漏了「從別的語言過來」那份，跟 `路線圖.md:70-76` 五份索引的表對不上 — 補上 — [疑]
- [低][結構] `docs/主題與-spork-索引.md:5` — 寫「日常會用到的 18–26」，README:35 與路線圖:72 寫 18–26b — 統一成 18–26b — [疑]
- [低][結構] `docs/怎麼做-X.md:35` — 「編成單一執行檔」指 05c、16d；05c 主題是 rule／不重編（只在講時間戳時提到 `declare-executable`），真正教「怎麼生出執行檔專案」的是 `05b-建立新專案.md:13-25` — 改指 05b、16d（05c 留作「build 沒反應」那列） — [實測 grep]
- [低][範例] `docs/怎麼做-X.md:31` — 「終端上色、進度條（接管線自動關掉）」指 39，39 只有一句「接到人就可以上色、畫進度條」（:57），沒有進度條範例 — 簡介改「終端上色（接管線自動關掉）」 — [實測 grep]

抽樣對照 OK 的（不列問題，供彙整者知道查過）：README:23/25/30/32/39/40/44/46/47/48/50/63/65；語言細節:13/14/16/17/19/20/21/22/23；主題:12/29/30/32/36/40/42/44/47；從別的語言:15/44/45/48/49（46f「25 列」實數 24 列，差一）；怎麼做-X-b:13/32/44。`os/shell` 回 768（3×256）、`os/date` 月與日 0-based 皆 [實測]。

## 沒來得及看的

- 路線圖:80「幾乎每個 ⚠ 都在對應範例裡跑得到」沒有逐條驗。
- 主題索引 27「51 個模組」未跟 syspath 實數對（`ls .../spork` 得 69 個檔案含 .so 與子目錄，需要人工算）。
- 怎麼做-X 兩支的「抄」欄 snippets 只驗存在（md-bundle），沒打開對照內容。
- 使用者關機，到此收尾。
