# D11 docs 27–31（spork 篆）審閱

審閱者：fable｜日期 2026-09-26｜範圍：`docs/27-spork-全覽.md`、`docs/28-spork-misc-順手工具.md`、`docs/28b-spork-misc-文字與流程.md`、`docs/29-spork-資料與文字.md`、`docs/30-spork-並行與服務.md`、`docs/31-spork-產生-html.md`

## 總評（3～6 句）
（收尾時補）

## 發現
### 27-spork-全覽
- [中][錯誤] `docs/27-spork-全覽.md:12` — 「在 REPL 裡 `(doc spork/misc/map-vals)`」照打會得到 `symbol spork/misc/map-vals not found.`，`doc` 查的是當前環境的綁定，得先 `(import spork/misc)` 再 `(doc misc/map-vals)` — 改成兩步寫法 — [實測]
- [中][錯誤] `docs/27-spork-全覽.md:4,36,42,52` — 「51 個模組／9 原生／42 純 janet」是把 `init.janet`（只是傘）與 `gfx2d-codegen`／`gfx2d-shader`（gfx2d 的內部件）都算成模組；本機 `ls syspath/spork` 是 41 支純 `.janet`（含 init 才 42）＋9 支 `.so`。而「七類地圖」表只列了 47 個名字，**`argparse` 不在地圖裡**（只出現在下面「五個專篇」表） — 把 `argparse` 補進「系統與檔案」列，並註明 51 含 init 與 gfx2d 兩支內部件 — [實測]
- [低][錯誤] `docs/27-spork-全覽.md:23-24` — 「702 變成 1409」本機實測 `janet -e` 是 708→1415（差值 707 相同，基底隨 Janet 版本／環境變） — 改寫成「多出 707 個」而不是給兩個絕對數 — [實測]
- [低][文風] `docs/27-spork-全覽.md:3,22-24,44,64` — 全篇破折號（——）當停頓用了 6 次，`writing.md` 明列為 AI 味 — 換句號 — [疑]
