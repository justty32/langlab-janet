# 從別的語言過來 · 索引

[← 教學目錄](README.md)｜[路線圖](路線圖.md)｜[我要做 X，去哪](怎麼做-X.md)

已經會一門語言，只想知道「我熟的那招在 Janet 怎麼寫」的話，從這裡進。
每篇都是逐條對照，⚠ 標的是**照直覺搬會出錯**的地方。

先看 [01d](01d-提早離開-return-break-continue.md)：`return`／`break`／`continue` 是
每個人第一天就會撞到的，四種語言共通。

## 先解決控制流

| # | 篇 | 重點 |
|---|----|------|
| 01d | [提早離開：return、break、continue](01d-提早離開-return-break-continue.md) | ★ 沒有 C 的 `return`／`continue`：最後運算式就是回傳值、函式 body 裡 `break` 即 return、⚠ **`(break 值)` 在迴圈裡值被丟掉**、⚠ `defer`／`try` 裡 `break` 跳不出外層迴圈、`label`＋`return` 跳巢狀 |

## C／C++

| # | 篇 | 重點 |
|---|----|------|
| 01b | [給 C++ 開發者的對照](01b-給-C++-開發者.md) | ★ 概念對照表、`=` 的兩套語意、沒有值拷貝、沒有重載、GC 與 RAII |
| 43 | [從 C／C++ 過來](43-從-C-C++-過來.md) | 日常語法逐條對照：全 double、string／buffer、可變參數、`main`／`argv`、assert |
| 43b | [從 C：控制流與位元](43b-從-C-控制流與位元.md) | switch→`case`、三段式 for、do-while、位元運算是 32 位元、enum→keyword、⚠ **`brushift` 拒收負數** |
| 43c | [從 C：函式、模組與 IO](43c-從-C-函式模組與-IO.md) | 標頭檔→`import`、static→`defn-`、stdin／stderr、⚠ **`%d` 遇 3.5 報錯** |

## Lua

| # | 篇 | 重點 |
|---|----|------|
| 44 | [從 Lua 過來](44-從-Lua-過來.md) | 0-based、`#`→`length`、多重回傳→解構、`s:sub`→`string/slice`、⚠ **`%` 是 C 那種、`mod` 才是 Lua 的** |
| 44b | [從 Lua：閉包、metatable、協程](44b-從-Lua-閉包-metatable-協程.md) | metatable 只有 `__index` 有對應、pcall→`protect`、coroutine→fiber |

## Go

| # | 篇 | 重點 |
|---|----|------|
| 45 | [從 Go 過來](45-從-Go-過來.md) | `err` 慣例三種寫法、`defer` 順序相反、⚠ **slice 是拷貝**、interface→鴨子、⚠ **`%t` 印的是型別名** |
| 45b | [從 Go：goroutine 與 channel](45b-從-Go-goroutine-與-channel.md) | ev 是單執行緒、⚠ **close＋range 會掉資料**、`select`／cancel、`ev/thread` 回 nil |

## Python

| # | 篇 | 重點 |
|---|----|------|
| 46 | [從 Python 過來](46-從-Python-過來.md) | ★ 對照表、六個 Python 直覺會害你的地方（`0` 是真、`=` 比身分、字串是 bytes、nil 存不進 table） |
| 46b | [Python：資料與推導](46b-Python-資料與推導.md) | 容器對照（⚠ 括號對調）、沒有 set、⚠ 切片負索引差一格、`seq`／`tabseq`／`generate` |
| 46c | [Python：字典與字串](46c-Python-字典與字串.md) | `dict.get`／`Counter`／`groupby`、⚠ 字串函式參數順序跟 Python 相反、反引號＝多行＋raw |
| 46d | [Python：函式](46d-Python-函式.md) | `*args`／`**kwargs` 對 `;`、decorator 用高階函式或巨集、generator 對 fiber、itertools 對照 |
| 46e | [Python：錯誤與物件](46e-Python-錯誤與物件.md) | `try/except/finally` 對 `try`／`defer`、自訂例外＝丟 table、class 對 prototype、⚠ `type` 一律回 `:table` |
| 46f | [Python：工具鏈](46f-Python-工具鏈.md) | import／`main`、pip 對 jpm、25 列標準函式庫對照、「Janet 就是沒有的」清單 |

## 可跑的對照範例

`examples/` 裡每種語言都有一支逐列印出「原本怎麼寫 → Janet 怎麼寫 → 實際跑出來是什麼」：
[`early-exit`](../examples/early-exit.janet)、[`compare-c`](../examples/compare-c.janet)、
[`compare-lua`](../examples/compare-lua.janet)、[`compare-go`](../examples/compare-go.janet)、
[`compare-python`](../examples/compare-python.janet)（另有
[`-fn`](../examples/compare-python-fn.janet)、[`-oop`](../examples/compare-python-oop.janet)）。

讀完之後照 [路線圖](路線圖.md) 接回正規順序。
