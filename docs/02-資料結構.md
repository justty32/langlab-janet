# 02 · 資料結構（array / tuple / table / struct）

Janet 的集合分「可變」與「不可變」兩套，一個 `@` 就是差別：

| 概念 | 可變（前綴 `@`） | 不可變 | 你熟悉的對應 |
|------|------------------|--------|--------------|
| 陣列 | `@[1 2 3]` array | `[1 2 3]` tuple | list / array |
| 雜湊表 | `@{:a 1}` table | `{:a 1}` struct | hash map / dict |

規則：**要改就用 `@`（array / table），當常數就用不可變（tuple / struct）**。
不可變的 struct / tuple 可以當 table 的 key，可變的不行。

## 陣列 array `@[...]`

```janet
(def arr @[1 2 3])
(array/push arr 4)          # arr 變 @[1 2 3 4]（原地改）
(arr 0)                     # => 1        用索引當函式呼叫來取值
(length arr)                # => 4
(array/slice arr 1 3)       # => @[2 3]   取子陣列

(map |(* $ $) arr)          # => @[1 4 9 16]
(filter even? arr)          # => @[2 4]
(reduce + 0 arr)            # => 10
```

tuple 是不可變版，取值一樣 `(tup 2)`，但不能 push / 改。

## 雜湊表 table `@{...}`

```janet
(def t @{:name "Alice" :age 30})
(put t :city "Taipei")      # 原地新增 / 覆寫一個 key
(t :name)                   # => "Alice"   把 key 當函式呼叫來取
(get t :age)                # => 30        get 也行，key 不存在回 nil
(get t :xxx :fallback)      # => :fallback 第三參數是預設值
(has-key? t :city)          # => true
(update t :age inc)         # 用函式更新：age 30 -> 31
```

看整張表的 key / value / pair：

```janet
(keys t)                    # => @[:age :name :city]
(values t)                  # => @[30 "Alice" "Taipei"]
(pairs t)                   # => @[(:age 30) (:name "Alice") (:city "Taipei")]
```

**地雷**：取「巢狀」的值不要寫 `(t :a :b)`——第二個參數是**預設值**不是下一層 key。
要鑽多層用 `get-in`：

```janet
(def nested @{:user @{:names @["Zed"]}})
(get-in nested [:user :names 0])   # => "Zed"
(put-in nested [:user :age] 5)     # 深層寫入
```

## struct `{...}`（不可變 table）

```janet
(def s {:a 1 :b 2})
(s :a)                      # => 1
(merge {:a 1} {:b 2})       # => @{:a 1 :b 2}   合併成新 table（右邊蓋左邊）
```

`merge` 回傳的是 table（可變）。要不可變版用 `(struct ;(kvs ...))` 或 `table/to-struct`。

## 走訪

```janet
(each v @[10 20 30] (prin v " "))          # 值：10 20 30
(eachp [k v] @{:a 1 :b 2} (print k v))     # key-value 對
(seq [[k v] :pairs @{:a 1 :b 2}] [k v])    # 推導出 pair 陣列
```

## ⚠ 運算子不會幫你轉型

Janet 的算術運算子**只吃數字**。從 Python／JS 過來的人最常踩的第一個坑是拿 `+` 接字串：

```janet
(+ "Hello! " "world")
# error: could not find method :+ for "Hello! "
```

錯誤訊息長得很奇怪（`could not find method :+`）——因為 `+` 對非數字會去找該型別的 `:+` 方法，而字串沒有。**認得這個訊息就等於認得這個坑。**

字串串接用 `string`，它吃任何東西、通通轉成字串：

```janet
(string "Hello! " "world")        # => "Hello! world"
(string "共 " 3 " 個")             # => "共 3 個"      ← 數字也吃
(string/join @["a" "b"] ", ")     # => "a, b"        ← 要分隔符用這個
```

| 想做 | Python／JS | Janet |
|------|-----------|-------|
| 數字相加 | `1 + 2` | `(+ 1 2)` |
| 字串接字串 | `"a" + "b"` | `(string "a" "b")` |
| 數字轉字串 | `str(3)` | `(string 3)` |

同理 `(= "1" 1)` 是 `false`：Janet 沒有隱式轉型，跨型別比較一律 false（見 [13](13-symbol-keyword-字串.md)）。

## 小抄

| 想做 | 陣列 | 表 |
|------|------|-----|
| 建立 | `@[...]` | `@{...}` |
| 取值 | `(arr i)` | `(t k)` / `(get t k dflt)` |
| 新增 / 改 | `(array/push arr v)` | `(put t k v)` |
| 巢狀取 / 寫 | `(get-in a [i j])` | `(get-in t [k1 k2])` / `put-in` |
| 長度 | `(length arr)` | `(length t)` |
| 全部 key / value | — | `(keys t)` / `(values t)` |
| 走訪 | `(each v arr ...)` | `(eachp [k v] t ...)` |

## 方法呼叫語法：keyword 放在呼叫位置

```janet
(def obj @{:greet (fn [self who] (string "hi " who))})
(:greet obj "you")     # => "hi you"
```

`(:key obj args…)` 等於「去 obj 裡拿 `:key` 這個函式，把 obj 自己當第一個參數傳進去」。
所以 handler 的第一個參數要留給 `self`——**忘了寫 self 是最常見的錯**：

```janet
(def bad @{:inc (fn [n] (+ n 1))})
(:inc bad 5)     # ✗ error: called with 2 arguments, expected 1
                 #   因為實際傳進去的是 (bad 5) 兩個參數
```

檔案、socket、子行程都吃這套：`(:read f :all)`、`(:close conn)`、`(:write (p :in) s)`。

## prototype：Janet 的「繼承」

table 可以指定一張 prototype，查不到的 key 就往上找：

```janet
(def Animal @{:speak (fn [self] "...") :name "?"})
(def Dog (table/setproto @{:speak (fn [self] (string (self :name) " 汪"))} Animal))
(def d   (table/setproto @{:name "小黑"} Dog))
(:speak d)              # => "小黑 汪"
(table/getproto d)      # => Dog
```

三層查找：`d` 自己 → `Dog` → `Animal`。這就是 Janet 全部的 OOP——沒有 class 關鍵字，
`(curenv)` 的環境繼承、模組、`make-env` 用的也都是同一個機制。

`(get t k)` 會走 prototype 鏈；只想看**自己這層**用 `(table/rawget t k)`。

下一步：[03-json.md](03-json.md)。
